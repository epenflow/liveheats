const Navbar = () => {
	return <header>navbar</header>;
};
const resources = {
	navigation: [
		{ href: "#", label: "Login" },
		{ href: "#", label: "Register" },
	] as Array<{ href: string; label: string }>,
};
export default Navbar;
